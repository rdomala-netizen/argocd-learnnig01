apiVersion: v1
kind: Service
metadata:
  name: gitops-demo
  namespace: argocd-learnning01
  labels:
    app: gitops-demo
spec:
  type: NodePort
  selector:
    app: gitops-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
