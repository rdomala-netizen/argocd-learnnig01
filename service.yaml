apiVersion: v1
kind: Service
metadata:
  name: 
  namespace: argocd-learnning01
  labels:
    app: 
spec:
  type: NodePort
  selector:
    app: 
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
